import { useEffect,useState } from "react"; import DashboardLayout from "../layouts/DashboardLayout"; import { api } from "../services/api";
export default function AdminEnquiries(){const [items,setItems]=useState([]);const load=()=>api.get("/enquiries").then(r=>setItems(r.data));useEffect(()=>{load()},[]);const status=async(id,value)=>{await api.put(`/enquiries/${id}`,{status:value});load()};return <DashboardLayout admin><h1 className="text-3xl font-extrabold">Enquiries</h1><div className="mt-7 overflow-x-auto card"><table className="w-full min-w-[700px] text-left text-sm"><thead className="border-b bg-slate-50"><tr><th className="p-4">Student</th><th>Contact</th><th>Course</th><th>Status</th><th>Received</th></tr></thead><tbody>{items.map(x=><tr className="border-b last:border-0" key={x._id}><td className="p-4 font-semibold">{x.name}</td><td>{x.email}<br/>{x.phone}</td><td>{x.course||"—"}</td><td><select value={x.status} onChange={e=>status(x._id,e.target.value)} className="rounded-lg border p-2"><option>new</option><option>contacted</option><option>closed</option></select></td><td>{new Date(x.createdAt).toLocaleDateString()}</td></tr>)}</tbody></table>{!items.length&&<p className="p-8 text-center text-slate-500">No enquiries yet.</p>}</div></DashboardLayout>}
