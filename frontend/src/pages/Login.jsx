import { useState } from "react"; import { Link, useNavigate } from "react-router-dom"; import { useAuth } from "../context/AuthContext";
export default function Login(){const [email,setEmail]=useState("student@studypoint.com"),[password,setPassword]=useState("Student@123"),[error,setError]=useState("");const {login}=useAuth();const nav=useNavigate();const submit=async e=>{e.preventDefault();try{const u=await login(email,password);nav(u.role==="admin"?"/admin":"/dashboard")}catch(e){setError(e.response?.data?.message||"Login failed")}};return <Auth title="Welcome back" subtitle="Sign in to continue your learning journey"><form onSubmit={submit} className="space-y-4"><Field label="Email" value={email} set={setEmail}/><Field label="Password" type="password" value={password} set={setPassword}/>{error&&<p className="text-sm text-red-600">{error}</p>}<button className="btn-primary w-full">Sign in</button><p className="text-center text-sm text-slate-500">New to Study Point? <Link className="font-semibold text-indigo-600" to="/register">Create account</Link></p></form></Auth>}
function Field({label,type="text",value,set}){return <label className="block text-sm font-semibold">{label}<input required type={type} value={value} onChange={e=>set(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3 outline-none focus:border-indigo-500"/></label>}
function Auth({title,subtitle,children}){return <div className="grid min-h-[calc(100vh-80px)] place-items-center bg-slate-100 p-5"><div className="w-full max-w-md rounded-3xl bg-white p-7 shadow-xl"><div className="mb-7 text-center"><div className="mx-auto grid h-12 w-12 place-items-center rounded-xl bg-indigo-600 font-bold text-white">SP</div><h1 className="mt-5 text-2xl font-extrabold">{title}</h1><p className="mt-2 text-sm text-slate-500">{subtitle}</p></div>{children}</div></div>}
