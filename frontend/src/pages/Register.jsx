import { useState } from "react"; import { Link,useNavigate } from "react-router-dom"; import { useAuth } from "../context/AuthContext";
export default function Register(){const [form,setForm]=useState({name:"",email:"",phone:"",password:""}),[error,setError]=useState("");const {register}=useAuth();const nav=useNavigate();const submit=async e=>{e.preventDefault();try{await register(form);nav("/dashboard")}catch(e){setError(e.response?.data?.message||"Registration failed")}};return <div className="grid min-h-[calc(100vh-80px)] place-items-center bg-slate-100 p-5"><div className="w-full max-w-md rounded-3xl bg-white p-7 shadow-xl"><div className="text-center"><div className="mx-auto grid h-12 w-12 place-items-center rounded-xl bg-indigo-600 font-bold text-white">SP</div><h1 className="mt-5 text-2xl font-extrabold">Create your account</h1><p className="mt-2 text-sm text-slate-500">Start your Study Point journey.</p></div><form onSubmit={submit} className="mt-7 space-y-4">{[["name","Full name"],["email","Email"],["phone","Phone"],["password","Password"]].map(([k,l])=><label key={k} className="block text-sm font-semibold">{l}<input required={k!=="phone"} type={k==="password"?"password":"text"} value={form[k]} onChange={e=>setForm({...form,[k]:e.target.value})} className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3 outline-none focus:border-indigo-500"/></label>)}{error&&<p className="text-sm text-red-600">{error}</p>}<button className="btn-primary w-full">Create account</button><p className="text-center text-sm text-slate-500">Already registered? <Link className="font-semibold text-indigo-600" to="/login">Sign in</Link></p></form></div></div>}
