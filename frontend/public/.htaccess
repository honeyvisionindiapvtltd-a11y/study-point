Options -MultiViews
RewriteEngine On

# Keep real files and directories available, then send React routes to the app shell.
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.html [L]
