PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/study_point
JWT_SECRET=replace_with_a_long_random_secret
JWT_EXPIRES_IN=7d
CLIENT_URL=http://localhost:5173

# In Render production, set CLIENT_URL=https://studypointbbsr.in.
# For multiple allowed origins, separate URLs with commas.

# SMTP settings for enquiry notifications.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=studypointbbsr@gmail.com
SMTP_PASS=replace_with_a_gmail_app_password
SMTP_FROM=studypointbbsr@gmail.com
