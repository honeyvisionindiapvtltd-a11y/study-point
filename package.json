{
  "name": "study-point-mern",
  "private": true,
  "version": "1.0.0",
  "workspaces": ["frontend"],
  "scripts": {
    "dev": "npm run dev --workspace frontend",
    "dev:full": "concurrently \"npm run dev --prefix backend\" \"npm run dev --workspace frontend\"",
    "build": "npm run build --workspace frontend",
    "start": "npm start --workspace frontend",
    "start:backend": "npm start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}